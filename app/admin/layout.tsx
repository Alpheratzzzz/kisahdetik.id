export const metadata = {
  title: "Admin | kisahdetik.id",
  description: "Panel admin untuk mengelola konten website",
};

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
